{
  "dependencies": {
    "bootstrap": "^5.3.1",
    "sass" : "^1.63.6"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass",
    "sass:watch": "sass --watch scss/styles.scss  styles/main.css"
  }
}
